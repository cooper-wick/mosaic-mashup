{
  "name": "mosaic-bubbles",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite --host",
    "build": "tsc && vite build",
    "build:zip": "npm run build && tar -czf dist.tar.gz -C dist . && echo 'Size (bytes):' && stat -f%z dist.tar.gz",
    "preview": "vite preview"
  },
  "devDependencies": {
    "typescript": "^5.2.2",
    "vite": "^5.0.0"
  }
}